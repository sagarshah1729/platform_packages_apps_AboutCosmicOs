<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Since the Galaxy kernel is not available on all devices the default is to not show the galaxy tab.
         The Galaxy tab should be enabled in device overlay. -->
    <bool name="device_has_galaxy">false</bool>
</resources>
